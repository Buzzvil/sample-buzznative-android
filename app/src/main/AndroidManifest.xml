<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="sample.buzznative.buzzvil.com.buzznativesample">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".NativeAdActivity" />
        <activity android:name=".NativeAdRecyclerViewActivity" />

        <!-- Admob integration -->
        <!-- Sample AdMob App ID: ca-app-pub-3940256099942544~3347511713 -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="[YOUR_ADMOB_AD_UNIT_ID]" />

        <!-- Mopub integration -->
        <meta-data
            android:name="com.buzzvil.mediation.mopub.ad_unit_id"
            android:value="[YOUR_MOPUB_AD_UNIT_ID]" />
    </application>
</manifest>
